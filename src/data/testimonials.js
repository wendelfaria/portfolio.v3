/**
 * Depoimentos de clientes.
 * Adicione aqui os depoimentos reais após recebê-los.
 *
 * Estrutura:
 * {
 *   id: number,
 *   name: string,
 *   role: string,      // ex: 'Psicóloga Clínica'
 *   text: string,
 *   avatar: string,    // caminho relativo a /public/images/ (ou null para usar iniciais)
 * }
 */
export const testimonials = []
