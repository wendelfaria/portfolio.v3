/**
 * Lista de projetos do portfólio.
 * Adicione aqui os seus cases reais conforme forem sendo concluídos.
 *
 * Estrutura de cada projeto:
 * {
 *   id: number,
 *   slug: string,
 *   title: string,
 *   niche: string,           // ex: 'Psicologia', 'Odontologia'
 *   nicheSlug: string,       // ex: 'psicologos'
 *   description: string,
 *   image: string,           // caminho relativo a /public/images/projects/
 *   liveUrl: string | null,  // URL do site ao vivo (ou null se offline)
 *   tags: string[],
 * }
 */
export const projects = []
